!!! 5
/[if lt IE 7]<html class="no-js ie6" lang="en">
/[if IE 7]<html class="no-js ie7" lang="en">
/[if IE 8]<html class="no-js ie8" lang="en">
/[if (gte IE 9)|!(IE)]<!--><html class="no-js" lang="en">
%head
  %meta{ :charset => "utf-8" }

  / Always force latest IE rendering engine (even in intranet) & Chrome Frame
  %meta{ :content => "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible" }

  / set the viewport for mobile devices
  %meta{ :name => 'viewport', :content => 'width=device-width, initial-scale=1.0' }

  %title= data.page.title || "Glycemia"

  = stylesheet_link_tag "site", "leftsidebar"

  /All JavaScript at the bottom, except for Modernizr which enables HTML5 elements & feature detects
  = javascript_include_tag "vendor/modernizr-2.0.6.min.js"
  = javascript_include_tag "vendor/jquery-1.7.2.js"

  = yield_content :head

%body{ :class => page_classes }
  = partial "partials/navbar"

  .container
    #content
      .sidebar
        / Sidebar content
        = yield_content :sidebar

      .main
        / Body content
        = yield

    = partial "partials/footer"
  / ! end of #container

  / js scripts
  = javascript_include_tag "site"
